@model IEnumerable<Profile>


<h2>Users</h2>
<hr />

<div class="container">
    <div class="row">
        @foreach (Profile p in Model)
        {
            <div class="col-md-2 mb-2">
                <partial name="_PartialSearch.cshtml" for="@p" />
            </div>
        }
    </div>
</div>